# Network & Event Loop Optimization (Phase 1)
uvloop==0.19.0
websockets==12.0
aiohttp==3.9.5

# Math Acceleration (Phase 2)
numba==0.59.1
numpy==1.26.4

# ML Dependencies — full 5-model ensemble (RF + GB + LGBM + XGB + CatBoost)
scikit-learn>=1.3.0
lightgbm>=4.3.0
xgboost>=2.0.0     # v18.8: 4th ensemble model (was missing → ML ran 3/5)
# catboost>=1.2.0  # v18.9.3: DISABLED — ~300MB RAM on the 1GB VM for negligible accuracy gain. Re-enable here + set BINBOT_USE_CATBOOST=1.
# NOTE: xgboost+catboost add ~250MB RAM. v18.8 frees that by NOT loading torch at startup
# (LSTM is off). If the VM still OOMs, raise MemoryMax in the systemd unit or drop catboost.

# Database & Misc (Existing & Phase 4)
python-binance>=1.0.19
sqlalchemy>=2.0.0
aiosqlite
openai
